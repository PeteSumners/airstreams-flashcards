# Video processing
opencv-python>=4.8.0
numpy>=1.24.0

# CLI and utilities
click>=8.1.0
tqdm>=4.66.0

# Claude API (for automated processing)
anthropic>=0.40.0

# Image processing (optional - only needed for SSIM deduplication method)
# scikit-image>=0.21.0  # Use --dedup-method ssim to enable
